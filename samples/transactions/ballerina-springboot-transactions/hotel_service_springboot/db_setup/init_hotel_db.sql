CREATE DATABASE IF NOT EXISTS hotel_db;

USE hotel_db;

create table IF NOT EXISTS BOOKINGS(ID serial, FIRST_NAME varchar(20) NOT NULL);

create table IF NOT EXISTS ROOMS (ID serial, ROOM_NO INT NOT NULL);